# Disable dependency lifecycle scripts unless a project command explicitly
# invokes the required work.
ignore-scripts=true

# Delay newly published dependencies long enough for ecosystem review.
min-release-age=14
